<meta charset="utf-8"/>
<title>Home</title>
<meta http-equiv="X-UA-Compatible" content="IE=edge"/>
<meta name="viewport" content="width=device-width, initial-scale=1"/>
<!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags-->
<meta name="description" content=""/>
<meta name="author" content=""/>
<link rel="icon" href="../../favicon.ico"/>
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
<link rel='stylesheet' href='/stylesheets/style.css' />
<?php
    $db = mysqli_connect('hostname','username','password','databasename');
    ?>
<script type="text/JavaScript" src="template.js"></script>
</head>

<body style="background-color:slategray">

<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Project name</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li class="active"><a href="/account">Home</a></li>
                <li><a href="/account/about">About</a></li>
                <li><a href="/cart">Cart</a></li>
            </ul>
        </div><!--/.nav-collapse -->
    </div>
</nav>
<div>
    <% if(rs.length > 0) { %>
    <table border="1" class="table table-striped">
        <tr><th>First Name</th>
            <th>Last Name</th>
            <th>Username</th>
            <th>Password</th>
            <th>Email</th></tr>

        <% for (var i=0; rs.length > i; i++) { %>
        <tr>
            <td><a href="/account/?a_id=<%= rs[i].a_id%>">
                    <%= rs[i].fname %></a></td>
            <td><a href="/account/?a_id=<%= rs[i].a_id%>"><%= rs[i].lname %></a></td>
            <td><a href="/account/?a_id=<%= rs[i].a_id%>"><%= rs[i].username%></a></td>
            <td><a href="/account/?a_id=<%= rs[i].a_id%>"><%= rs[i].passwords %></a></td>
            <td><a href="/account/?a_id=<%= rs[i].a_id%>"><%= rs[i].email %></a></td>
        </tr>

        <% } %>
    </table>
    <% } else { %>
    No accounts are entered into the Account database table.
    <% } %>
</div>
</body>
</html>